<section class="container">
    <div class="container mt-5">
        {{!-- Exibição das mensagens flash de sucesso --}}
        {{#if sucess_msg}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{sucess_msg}}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        {{/if}}
    </div>
    <div class="row my-2">
        {{#if error_msg}}
        <div class="alert alert-danger alert-dismissible fade show col-md-6 mx-auto" role="alert">
            {{error_msg}}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        {{/if}}
    </div>
    {{!-- Exibição das mensagens de flash de sucesso --}}

    <section class="container">
        <!-- Cabeçalho -->
        <section class="d-flex justify-content-between mt-4">
            <h2>Página inicial dos Autores</h2>
            <a href="/autores/create" class="btn btn-primary">Cadastrar novo Autor</a>
        </section>

        <hr>

        <!-- Lista de Autores -->
        <section class="container my-4">
            <div class="row">
                {{#each dados}}
                <div class="col-md-4 mb-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body">
                            <h5 class="card-title">Nome: {{dataValues.nome}}</h5>
                            <p class="card-text"><strong>ID:</strong> {{dataValues.id}}</p>
                            <p class="card-text"><strong>Nacionalidade:</strong> {{dataValues.nacionalidade}}</p>
                            <p class="card-text"><strong>Data de Nascimento:</strong> {{dataValues.data_nascimento}}</p>
                            <div class="d-flex justify-content-between">
                                <a href="" class="btn btn-sm btn-danger" data-bs-toggle="modal"
                                    data-bs-target="#modal{{dataValues.id}}">Excluir</a>
                            </div>
                        </div>
                    </div>
                </div>

                {{>modalDelete2 idItem = dataValues.id}}

                {{/each}}
            </div>
        </section>
    </section>
</section>